apiVersion: v1
kind: Service
metadata:
  name: wecod
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wecod"
    port: 8080
  selector:
    app: wecod
