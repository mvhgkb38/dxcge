apiVersion: v1
kind: Service
metadata:
  name: recok
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "recok"
    port: 8080
  selector:
    app: recok
